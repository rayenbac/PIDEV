{% extends 'dashboard.html.twig' %}
{% block body %}
<div class="container-fluid" id="container-wrapper">
          <div class="d-sm-flex align-items-center justify-content-between mb-4">
            <h1 class="h3 mb-0 text-gray-800">Forum</h1>
            <ol class="breadcrumb">
              <li class="breadcrumb-item"><a href="./">Home</a></li>
              <li class="breadcrumb-item">Forum</li>
              <li class="breadcrumb-item active" aria-current="page">Actualité</li>
            </ol>
          </div>

          <div class="row">
            <div class="col-lg-12 mb-4">
              <!-- Liste des questions&réponses -->
              <div class="card">
                <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                  <h6 class="m-0 font-weight-bold text-primary">Liste des moods</h6>
                </div>
                <div class="table-responsive">
                  <table class="table align-items-center table-flush">
                    <thead class="thead-light">
                      <tr>
                        <th>IdMood</th>
                            <th>IdUser</th>
                            <th>Votre mood</th>
                            <th>Description</th>
                            <th>Date de l'enregistrement</th>
                            <th>Modifier</th>
                            <th>Supprimer</th>
                                  
                      </tr>
                    </thead>
                    <tbody>
                      <tr>
                     </tr>
                      <tr>
                        <tr>
                   {% for i in m %}
                   <tr>
                        <td>{{i.MoodId}}</td>
                            <td>{{i.UserId}}</td>
                            <td>{{i.Mood}}</td>
                            <td>{{i.Description}}</td>
                            <td>{{i.date|date('Y-m-d H:i:s')}}</td>
                
   
 
                
                
                  <td>
  <a href="{{ path('editmood', {'id': i.id}) }}" class="btn btn-warning">
    <i class="fas fa-edit"></i> Edit
  </a>
</td>


                     <td><a href="{{path('deletemood',{'id' :i.id }) }}" class="btn btn-danger">
                    <i class="fas fa-trash"></i>
                  </a></td>


                      <td>
       <a class="btn btn-success" href="{{ path('pdf') }}">Create pdf</a> 
         </td>
         
         <td>
       <a class="btn btn-success" href="{{ path('mood_statistics') }}">Show Mood Statistics</a> 
         </td>
               
{#    
          <td><a href="{{path('afficheA',{'id':i.id }) }}" class="btn btn-sm btn-primary">listes des moods </a></td> #}
          
          
                {% endfor %}

  



                </tr>
                     
                    </tbody>
                  </table>
                </div>
                <div class="card-footer"></div>
              </div>
            </div>
          </div>




  






{% endblock %}