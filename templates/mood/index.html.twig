{% extends 'base.html.twig' %}

{% block stylesheets %}
  {{ parent() }}
{% endblock %}
{% block body %}
<section class="breadcrumb_part breadcrumb_bg">
        
          
                <div class="col-lg-12">
                    <div class="breadcrumb_iner">
                        <div class="breadcrumb_iner_item">
                            <h2>Liste des moods</h2>
                        </div>
                    </div>
                </div>
            
        
    </section>

<div id="google_translate_element"></div>
                                    <script type="text/javascript">
                                        function googleTranslateElementInit() {
                                            new google.translate.TranslateElement({pageLanguage: 'en'}, 'google_translate_element');
                                        }
                                    </script>

                                    <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>

<div class="page-container">
    <!-- Content Wrapper START -->
    <div class="main-content">
      <div class="card">
          <div class="card-body">
              <h4>Listes des Moods</h4>
              <div class="m-t-25">
                <table class="data-table table">
                    <thead>
                        <tr>
                        <th>IdMood</th>
                            <th>IdUser</th>
                            <th>Votre mood</th>
                            <th>Description</th>
                            <th>Modifier</th>
                            <th>Supprimer</th>
                            <th>PDF</th>
                            <th>Statistics</th>
                        </tr>
                    </thead>
                    <tbody>
                     {% for mood in m %}
                        <tr>
                        <td>{{mood.MoodId}}</td>
                            <td>{{mood.UserId}}</td>
                            <td>{{mood.Mood}}</td>
                            <td>{{mood.Description}}</td>
                           
                            
												
                        <td>
        
         <button class="btn btn-primary">
         <a href="{{ path('editmood',{'id':mood.id}) }}"style="color: white;">Update</a>
          </button>
        
      </td>
       <td><button class="btn btn-primary">
        <a href="{{path('deletemood',{'id' :mood.id }) }}"style="color: white;">Delete</a>
        </button> </td>

</td>
       <td>
       <a class="btn btn-success" href="{{ path('pdf') }}">Create pdf</a> 
         </td>
         
         <td>
       <a class="btn btn-success" href="{{ path('stat') }}">Show Mood Statistics</a> 
         </td>
         
    
											
                        </tr>
                        {% endfor %}
  
                     
                    </tbody>
                </table>
              </div>
          </div>
      </div>
    </div>

{% endblock %}
{% block javascripts %}
  {{ parent() }}
{% endblock %}