{% extends 'dashboard.html.twig' %}
{% block body %}
<div class="container-fluid" id="container-wrapper">
          <div class="d-sm-flex align-items-center justify-content-between mb-4">
            <h1 class="h3 mb-0 text-gray-800">Liste des réponses</h1>
            <ol class="breadcrumb">
              <li class="breadcrumb-item"><a href="./">Home</a></li>
              <li class="breadcrumb-item">Forum</li>
              <li class="breadcrumb-item active" aria-current="page">Actualité</li>
            </ol>
          </div>


<table  border="1">
 <div class="row">
            <div class="col-lg-12 mb-4">
              <!-- Liste des questions&réponses -->
              <div class="card">
                <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                  <h6 class="m-0 font-weight-bold text-primary">Liste des questions&réponses</h6>
                </div>
                <div class="table-responsive">
                  <table class="table align-items-center table-flush">
                    <thead class="thead-light">
  <th>Id</th>
  <th>Reponse</th>
  <th>Suppression</th>

 </tr>
                    </thead>
                    <tbody>
                      <tr>
                     </tr>
                      <tr>
                        <tr>
<tbody>
{% for i in post %}
<tr>
  <td>{{i.id}}</td>

  <td>{{i.reponse}}</td>
    <td><a href="{{path('suppCommentaire',{'id':i.id }) }}" class="btn btn-danger">
                    <i class="fas fa-trash"></i>
                  </a></td>
   
 
</tr>
 {% endfor %}
<tbody/>

</table>
<footer class="sticky-footer bg-white">
        <div class="container my-auto">
          <div class="right text-center my-auto">
            <span>copyright &copy; <script> document.write(new Date().getFullYear()); </script> - developed by
              <b><a >Echkili</a></b>
            </span>
          </div>
        </div>
      </footer>
{% endblock %} 

