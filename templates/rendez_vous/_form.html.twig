{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
<div class="form-group">
    {{ form_row(form.nom) }}
    </div>
    <div class="form-group">
    {{ form_row(form.prenom) }}</div>
    <div class="form-group">
    {{ form_row(form.cause ) }}    </div>

      <div class="form-group">
    {{ form_row(form.description ) }}    </div>

       <div class="form-group">
    {{ form_row(form.dateRV ) }}    </div>

        <div class="form-group">
    {{ form_row(form.medecin ) }}
    
    
        </div>

    <button class="btn_1 d-none d-lg-block" >{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
