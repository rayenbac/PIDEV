{# {{form_start(f,{'attr': {'novalidate': 'novalidate'}} )}}
<table>


<tr>
<th>{{ form_label (f.DateReservation,"DateReservation") }} </th>
<th>{{ form_widget (f.DateReservation) }} </th>
<th>{{ form_errors (f.DateReservation) }} </th>
</tr>

<tr>
<th>{{ form_label (f.NombreDePlaceAReserver,"Nombre De Places ") }} </th>
<th>{{ form_widget (f.NombreDePlaceAReserver) }} </th>
<th>{{ form_errors (f.NombreDePlaceAReserver) }} </th>
</tr>

</table>

#}

{% extends "base.html.twig" %}



{% block body %}


<section class="regervation_part section_padding">
	<div class="container">
		<div class="row align-items-center regervation_content">
			<div class="col-lg-7">
				<div class="regervation_part_iner">
					
						
						<div class="card-body">
							{{ form_start(f, {'attr': {'novalidate': 'novalidate'}}) }}
							<div class="form-group">
								{{form_label(f.NombreDePlaceAReserver,"Nombre De Places A Reserver" )}}
								{{form_widget(f.NombreDePlaceAReserver,{'attr': {'class': 'form-control', 'placeholder': ' NombreDePlacesAReserver', 'id': 'exampleFormControlTextarea1', }}) }}
								{{form_errors(f.NombreDePlaceAReserver)}}
								{{e}}
							</div>
							<div class="form-group">
								{{form_label(f.Email,"Email" )}}
								{{form_widget(f.Email,{'attr': {'class': 'form-control', 'placeholder': ' Email', 'id': 'exampleFormControlTextarea1', }}) }}
								{{form_errors(f.Email)}}
							</div>
							<div class="form-group">
								{{form_label(f.Evenements,"Choisir l'événement " )}}
								{{form_widget(f.Evenements,{'attr': {'class': 'form-control', 'placeholder': ' NombreDePlacesAReserver', 'id': 'exampleFormControlTextarea1', }}) }}
								{{form_errors(f.Evenements)}}
							</div>
							{{ form_widget(f.save, { 'attr': {'class': 'btn btn-primary'} }) }}
    						{{form_end(f)}}
						</div>					
				</div>
			</div>
		</div>
	</div>

</section>


{% endblock %}

